# 006

## オーナーメモ

もともとはドキュメント読み込みとか全部まかせてたんだけど、まだ正確性に欠ける。ダメだ。
こういう系のアプリケーションは、自分で Postman で動作確認して要望を書くほうがいいや。
というわけで 006 についてはオーナーが自分で要望を記載した。

## 要望

### 新しい選択肢追加 "指定の月の平日に勤怠を詰め込む for yyyy-mm"

以下のような順番で処理

### コマンドライン引数で月を取得

`yyyy-mm`

### アクセストークン取得

これは実装済み。

### employee id, company id 取得

curl で動作確認済み。

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/users/me' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

```json
{
  "id": 111,
  "companies": [
    {
      "id": 222, // ここが company_id
      "name": "...",
      "role": "self_only",
      "external_cid": "333",
      "employee_id": 444, // ここが employee_id
      "display_name": "..."
    }
  ]
}
```

### 勤怠タグ id 取得

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/attendance_tags?company_id=<company_id>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

```json
{
  "employee_attendance_tags": [
    {
      "id": 555, // ここが勤怠タグ id
      "company_id": 222,
      "name": "出社（事務所／常駐先）", // "出社" と入っているものを取得する。
      "description": "テレワークと出社と混在する場合、出社日にタグを付与してください。",
      "max_amount": 1,
      "published": true,
      "is_employee_usable": true
    }
  ]
}
```

### この月のすべての日付に対して以下を行う

curl で動作確認済み。

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/work_records/<date>?company_id=<company_id>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

```json
{
  "date": "2026-02-21",
  "day_pattern": "normal_day", // ほか、 prescribed_holiday とかがある
  // ...
}
```

### normal_day だったら続行する

ほかはスキップ

### normal_day に勤怠を入れる

```bash
curl --location --request PUT 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/work_records/<date>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>' \
--data '{
  "company_id": <company_id>,
  "break_records": [
    {
      "clock_in_at": "<date> 11:30:00",
      "clock_out_at": "<date> 12:30:00"
    }
  ],
  "work_record_segments": [
    {
      "clock_in_at": "<date> 09:00:00",
      "clock_out_at": "<date> 19:00:00"
    }
  ]
}'
```

#### エラーだったら、ループを終了する

```json
{
  "status_code": 400,
  "errors": [
    {
      "type": "bad_request",
      "messages": [
        "給与明細が確定されているので、更新できません。"
      ]
    }
  ]
}
```

#### 成功だったら次の日へ

```json
{
  "break_records": [
    {
      "clock_in_at": "<date>T11:30:00.000+09:00",
      "clock_out_at": "<date>T12:30:00.000+09:00"
    }
  ],
  "clock_in_at": "<date>T09:00:00.000+09:00",
  "clock_out_at": "<date>T19:00:00.000+09:00",
  "date": "<date>",
  "day_pattern": "normal_day",
  "schedule_pattern": "",
  "early_leaving_mins": 0,
  "half_holiday_type": null,
  "half_paid_holiday_mins": 0,
  "half_special_holiday_mins": 0,
  "hourly_paid_holiday_mins": 0,
  "hourly_special_holiday_mins": 0,
  "is_absence": false,
  "is_editable": true,
  "lateness_mins": 0,
  "normal_work_clock_in_at": "<date>T09:00:00.000+09:00",
  "normal_work_clock_out_at": "<date>T18:00:00.000+09:00",
  "normal_work_mins": 480,
  "note": "",
  "paid_holiday": 0.0,
  "paid_holidays": [],
  "special_holiday": 0.0,
  "special_holiday_setting_id": null,
  "use_attendance_deduction": false,
  "use_default_work_pattern": true,
  "use_half_compensatory_holiday": false,
  "total_overtime_work_mins": 60,
  "total_prescribed_holiday_work_mins": 0,
  "total_holiday_work_mins": 0,
  "total_latenight_work_mins": 0,
  "not_auto_calc_work_time": false,
  "total_excess_statutory_work_mins": 0,
  "total_latenight_excess_statutory_work_mins": 0,
  "total_overtime_except_normal_work_mins": 60,
  "total_latenight_overtime_except_normal_work_mins": 0,
  "work_record_segments": [
    {
      "clock_in_at": "<date>T09:00:00.000+09:00",
      "clock_out_at": "<date>T19:00:00.000+09:00"
    }
  ]
}
```

### normal_day に勤怠タグを入れる

```bash
curl --location --request PUT 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/attendance_tags/2026-03-02' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>' \
--data '{
  "company_id": <company_id>,
  "employee_attendance_tags": [
    {
      "attendance_tag_id": <attendance_tag_id>,
      "amount": 1
    }
  ]
}'
```

```json
{
  "employee_attendance_tags": [
    {
      "attendance_tag": {
        "id": 555,
        "company_id": 222,
        "name": "出社（事務所／常駐先）",
        "description": "テレワークと出社と混在する場合、出社日にタグを付与してください。",
        "max_amount": 1,
        "published": true,
        "is_employee_usable": true
      },
      "amount": 1
    }
  ]
}
```

### 1件終わるごとにコンソールログがほしい

エラーもわかりやすく。

### すべての日付が終わったら終了

## 詳細設計

### この設計のゴール

指定月 (`yyyy-mm`) の全平日 (`normal_day`) について、freee 人事労務 API を使って以下を一括実行できるようにする。

- 各平日に `09:00-19:00` の勤怠レコードを登録する (休憩 `11:30-12:30` 含む)
- 各平日に「出社」勤怠タグを付与する
- 1 日ごとに進捗をコンソール出力し、エラー発生時はループを即座に中断する

005 が「1 日分の勤怠登録」だったのに対して、006 は「月単位の一括版」にあたる。
005 では API レスポンス構造が未確認だったから三段階方式 (事前取得 -> .env 反映 -> 本実行) で設計したけど、006 の要望ではオーナーが curl で全 API を実動作確認済みなので、レスポンス構造は確定している前提で進めるよ♡

### 処理フロー概要

```text
1. コマンドライン引数から yyyy-mm を取得
2. アクセストークン取得 (実装済み)
3. GET /hr/api/v1/users/me -> company_id, employee_id を取得
4. GET /hr/api/v1/employees/{employee_id}/attendance_tags -> 「出社」タグの id を取得
5. 対象月の全日付を生成 (1 日 ~ 末日)
6. 日付ループ開始
   6a. GET /hr/api/v1/employees/{employee_id}/work_records/{date} -> day_pattern を確認
   6b. day_pattern が normal_day でなければスキップ (ログ出力して次の日へ)
   6c. PUT /hr/api/v1/employees/{employee_id}/work_records/{date} -> 勤怠登録
   6d. エラーならループ中断 (終了コード 1)
   6e. PUT /hr/api/v1/employees/{employee_id}/attendance_tags/{date} -> タグ付与
   6f. エラーならループ中断 (終了コード 1)
   6g. 成功ログを出力して次の日へ
7. 全日付完了 -> サマリーログを出力して正常終了 (終了コード 0)
```

### 新規ファイル / モジュール構成

004 の方針に従い、`app/actions/` に 1 機能 1 モジュール、`app/clients/` に API 通信を配置する。

#### 新規ファイル

| ファイル | 責務 |
|---|---|
| `app/actions/bulk_attendance.py` | 月単位一括勤怠登録のユースケース。`handler(context: AppContext) -> int` を公開する |
| `app/clients/hr_api_client.py` | freee 人事労務 API (`/hr/api/v1/...`) への通信を担当する。`FreeeApiClient` をラップして人事労務固有のメソッドを提供する |

#### 既存ファイルの変更

| ファイル | 変更内容 |
|---|---|
| `app/actions/registry.py` | `ACTIONS` タプルに `bulk_attendance` の `ActionDefinition` を追加する |
| `app/clients/freee_api_client.py` | `put` メソッドを追加する (現状 `get` と `post` のみ) |

#### ファイル間の依存関係

```text
app/actions/bulk_attendance.py
    -> app/clients/hr_api_client.py   (人事労務 API 呼び出し)
    -> app/context.py                 (AppContext 経由で api_client, config を受け取る)
    -> app/errors.py                  (エラー型)
    -> app/exit_codes.py              (終了コード)

app/clients/hr_api_client.py
    -> app/clients/freee_api_client.py  (FreeeApiClient をラップ)
    -> app/errors.py                    (エラー型)
```

### API エンドポイント一覧

オーナーが curl で動作確認済みの 5 つの API を使う。全部人事労務 API (`/hr/api/v1/...`) 経由。

#### 1. ユーザー情報取得 (company_id, employee_id の確定)

- メソッド: `GET`
- パス: `/hr/api/v1/users/me`
- 用途: ログインユーザーの `company_id` と `employee_id` を取得する
- レスポンスから取る値:
    - `companies[0].id` -> `company_id`
    - `companies[0].employee_id` -> `employee_id`
- 注意: `companies` が複数ある場合は先頭を使う (将来的に選択 UI を入れてもよい)

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/users/me' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

#### 2. 勤怠タグ一覧取得 (出社タグ id の確定)

- メソッド: `GET`
- パス: `/hr/api/v1/employees/{employee_id}/attendance_tags`
- クエリパラメータ: `company_id={company_id}`
- 用途: 「出社」を含む勤怠タグの `id` を取得する
- 抽出ロジック: `employee_attendance_tags` 配列から `name` に `"出社"` を含むものを探す

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/attendance_tags?company_id=<company_id>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

#### 3. 勤怠レコード取得 (day_pattern の確認)

- メソッド: `GET`
- パス: `/hr/api/v1/employees/{employee_id}/work_records/{date}`
- クエリパラメータ: `company_id={company_id}`
- 用途: 対象日の `day_pattern` を確認し、`normal_day` かどうかを判定する
- `normal_day` 以外 (`prescribed_holiday` 等) はスキップ対象

```bash
curl --location 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/work_records/<date>?company_id=<company_id>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>'
```

#### 4. 勤怠レコード更新 (出勤・退勤・休憩の登録)

- メソッド: `PUT`
- パス: `/hr/api/v1/employees/{employee_id}/work_records/{date}`
- 用途: 対象日に `09:00-19:00` の勤怠と `11:30-12:30` の休憩を登録する
- リクエストボディ:

```json
{
    "company_id": "<company_id>",
    "break_records": [
        {
            "clock_in_at": "<date> 11:30:00",
            "clock_out_at": "<date> 12:30:00"
        }
    ],
    "work_record_segments": [
        {
            "clock_in_at": "<date> 09:00:00",
            "clock_out_at": "<date> 19:00:00"
        }
    ]
}
```

```bash
curl --location --request PUT 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/work_records/<date>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>' \
--data '{
    "company_id": <company_id>,
    "break_records": [
        {
            "clock_in_at": "<date> 11:30:00",
            "clock_out_at": "<date> 12:30:00"
        }
    ],
    "work_record_segments": [
        {
            "clock_in_at": "<date> 09:00:00",
            "clock_out_at": "<date> 19:00:00"
        }
    ]
}'
```

#### 5. 勤怠タグ更新 (出社タグの付与)

- メソッド: `PUT`
- パス: `/hr/api/v1/employees/{employee_id}/attendance_tags/{date}`
- 用途: 対象日に「出社」勤怠タグを付与する
- リクエストボディ:

```json
{
    "company_id": "<company_id>",
    "employee_attendance_tags": [
        {
            "attendance_tag_id": "<attendance_tag_id>",
            "amount": 1
        }
    ]
}
```

```bash
curl --location --request PUT 'https://api.freee.co.jp/hr/api/v1/employees/<employee_id>/attendance_tags/<date>' \
--header 'Accept: application/json' \
--header 'Authorization: Bearer <TOKEN>' \
--data '{
    "company_id": <company_id>,
    "employee_attendance_tags": [
        {
            "attendance_tag_id": <attendance_tag_id>,
            "amount": 1
        }
    ]
}'
```

### 各モジュールの詳細

#### `app/clients/freee_api_client.py` の変更

`put` メソッドを追加する。既存の `post` と同じパターンで、`method` だけ `"PUT"` に変える。

```python
def put(self, path: str, *, json_body: "dict[str, Any] | None" = None) -> ApiResponse:
    """PUT リクエストを実行します。"""
    return self._request("PUT", path, json=json_body)
```

#### `app/clients/hr_api_client.py` の設計

`FreeeApiClient` をラップして、人事労務 API 固有のメソッドを提供するクラス。
パス構築を一元化し、呼び出し元がパスを直書きしなくて済むようにする。

```python
class HrApiClient:
    """freee 人事労務 API のラッパーです。"""

    HR_BASE = "/hr/api/v1"

    def __init__(self, api_client: FreeeApiClient) -> None:
        self._client = api_client

    def get_current_user(self) -> ApiResponse:
        """ログインユーザー情報を取得します。"""
        return self._client.get(f"{self.HR_BASE}/users/me")

    def get_attendance_tags(self, employee_id: int, company_id: int) -> ApiResponse:
        """従業員の勤怠タグ一覧を取得します。"""
        return self._client.get(
            f"{self.HR_BASE}/employees/{employee_id}/attendance_tags",
            params={"company_id": company_id},
        )

    def get_work_record(self, employee_id: int, date: str, company_id: int) -> ApiResponse:
        """指定日の勤怠レコードを取得します。"""
        return self._client.get(
            f"{self.HR_BASE}/employees/{employee_id}/work_records/{date}",
            params={"company_id": company_id},
        )

    def put_work_record(self, employee_id: int, date: str, body: dict) -> ApiResponse:
        """指定日の勤怠レコードを更新します。"""
        return self._client.put(
            f"{self.HR_BASE}/employees/{employee_id}/work_records/{date}",
            json_body=body,
        )

    def put_attendance_tags(self, employee_id: int, date: str, body: dict) -> ApiResponse:
        """指定日の勤怠タグを更新します。"""
        return self._client.put(
            f"{self.HR_BASE}/employees/{employee_id}/attendance_tags/{date}",
            json_body=body,
        )
```

#### `app/actions/bulk_attendance.py` の設計

メインのユースケースモジュール。`handler(context: AppContext) -> int` を公開する。

処理の流れ:

1. `_parse_target_month(args)` -> `yyyy-mm` をパースして `(year, month)` を返す
2. `_resolve_ids(hr_client)` -> `company_id`, `employee_id`, `attendance_tag_id` を取得する
3. `_generate_dates(year, month)` -> 対象月の全日付リストを生成する (`yyyy-mm-dd` 形式)
4. `_process_date(hr_client, ...)` -> 1 日分の処理 (day_pattern 確認 -> 勤怠登録 -> タグ付与)
5. `handler` がこれらを呼んでループを回す

```python
def handler(context: AppContext) -> int:
    """指定月の平日に一括で勤怠を登録します。"""

    # 1. yyyy-mm をパース
    year, month = _parse_target_month()

    # 2. API クライアント準備
    hr_client = HrApiClient(context.api_client)

    # 3. company_id, employee_id 取得
    company_id, employee_id = _resolve_user_ids(hr_client)

    # 4. 出社タグ id 取得
    attendance_tag_id = _resolve_attendance_tag_id(hr_client, employee_id, company_id)

    # 5. 日付リスト生成
    dates = _generate_dates(year, month)

    # 6. 日付ループ
    success_count = 0
    skip_count = 0
    for date in dates:
        result = _process_date(hr_client, employee_id, company_id, attendance_tag_id, date)
        if result == "success":
            success_count += 1
        elif result == "skipped":
            skip_count += 1
        elif result == "error":
            # エラー時はループ中断
            _print_summary(success_count, skip_count, error_date=date)
            return EXIT_CODE_APP_ERROR

    # 7. 完了
    _print_summary(success_count, skip_count)
    return EXIT_CODE_OK
```

主要な内部関数:

- `_parse_target_month()`: `sys.argv` から `yyyy-mm` を取得してバリデーションする。不正なら即終了
- `_resolve_user_ids(hr_client)`: `GET /hr/api/v1/users/me` を呼び、`companies[0]` から `company_id` と `employee_id` を返す
- `_resolve_attendance_tag_id(hr_client, employee_id, company_id)`: 勤怠タグ一覧から `name` に `"出社"` を含むタグの `id` を返す。見つからなければ `ActionExecutionError` を投げる
- `_generate_dates(year, month)`: `calendar` モジュールで対象月の 1 日 ~ 末日を `yyyy-mm-dd` のリストで返す
- `_process_date(...)`: 1 日分の勤怠登録 + タグ付与を行い、`"success"` / `"skipped"` / `"error"` を返す

#### `_process_date` の詳細フロー

```text
1. GET work_records/{date} を呼ぶ
2. day_pattern を確認
   - "normal_day" でなければ "skipped" を返す (ログ: "[SKIP] 2026-03-01 (prescribed_holiday)")
3. PUT work_records/{date} で勤怠登録
   - 成功 (2xx) -> 次へ
   - エラー -> ログ出力して "error" を返す
4. PUT attendance_tags/{date} でタグ付与
   - 成功 (2xx) -> 次へ
   - エラー -> ログ出力して "error" を返す
5. ログ: "[OK] 2026-03-02 09:00-19:00 出社タグ付与済み"
6. "success" を返す
```

#### `_parse_target_month` の詳細

- `sys.argv` から `yyyy-mm` を探す (位置引数 or `--month` オプション、どちらでも可)
- 正規表現 `^\d{4}-\d{2}$` でフォーマット検証する
- `datetime` で年月の妥当性を検証する (例: `2026-13` は不正)
- 不正なら `sys.exit(EXIT_CODE_MENU_ERROR)` で終了する

引数の受け渡し方は、メニュー選択後にコンソールで入力を促す方式にする。
メニューで「指定の月の平日に勤怠を詰め込む」を選んだ後、`input()` で `yyyy-mm` を聞く形。

```text
対象月を入力してね (yyyy-mm): 2026-03
```

#### `app/actions/registry.py` への登録

```python
from app.actions.bulk_attendance import handler as bulk_attendance_handler

# ACTIONS タプルに追加
ActionDefinition(
    action_id="bulk_attendance",
    menu_label="指定の月の平日に勤怠を詰め込む",
    handler=bulk_attendance_handler,
),
```

### 勤怠登録の payload 構築

005 では API レスポンスが未確認だったから「観察して合わせる」方針だったけど、006 ではオーナーが curl で確認済みなので payload は固定で組む。

#### 勤怠レコード更新の payload

```python
def _build_work_record_payload(company_id: int, date: str) -> dict:
    """勤怠レコード更新用の payload を構築します。"""
    return {
        "company_id": company_id,
        "break_records": [
            {
                "clock_in_at": f"{date} 11:30:00",
                "clock_out_at": f"{date} 12:30:00",
            }
        ],
        "work_record_segments": [
            {
                "clock_in_at": f"{date} 09:00:00",
                "clock_out_at": f"{date} 19:00:00",
            }
        ],
    }
```

#### 勤怠タグ更新の payload

```python
def _build_attendance_tag_payload(company_id: int, attendance_tag_id: int) -> dict:
    """勤怠タグ更新用の payload を構築します。"""
    return {
        "company_id": company_id,
        "employee_attendance_tags": [
            {
                "attendance_tag_id": attendance_tag_id,
                "amount": 1,
            }
        ],
    }
```

### エラーハンドリング方針

#### API エラーの分類

| 状態 | 対応 |
|---|---|
| 401 Unauthorized | トークン期限切れ。`ApiAuthenticationError` が飛ぶ。`bootstrap` 層で捕捉される想定 |
| 403 Forbidden | 権限不足。ログにメッセージを出してループ中断。終了コード `1` |
| 400 Bad Request | 要望にある「給与明細が確定されているので、更新できません。」等。ログにエラーメッセージを出してループ中断。終了コード `1` |
| 404 Not Found | 従業員や勤怠レコードが見つからない。ログに出してループ中断。終了コード `1` |
| 5xx | サーバーエラー。ログに出してループ中断。終了コード `1` |
| 接続エラー | `ApiConnectionError` が飛ぶ。ログに出してループ中断。終了コード `1` |

#### エラー時のループ中断ルール

要望に「エラーだったら、ループを終了する」と明記されている。よって:

- 勤怠登録 (PUT work_records) でエラー -> 即中断
- タグ付与 (PUT attendance_tags) でエラー -> 即中断
- day_pattern 確認 (GET work_records) でエラー -> 即中断
- スキップ (normal_day 以外) はエラーではないので中断しない

#### `FreeeApiClient` の既存エラーハンドリングとの整合

現状の `FreeeApiClient._request` は 400 以上で `ApiResponseError` を raise する。
でも 006 では、400 レスポンスのボディに含まれる `errors[].messages` をユーザーに見せたい。

対応方針:

- `FreeeApiClient` に `put` メソッドを追加する際、エラーレスポンスのボディも取れるようにする
- 具体的には、`_request` メソッドで `status_code >= 400` の場合にもレスポンスボディをパースして例外に含める
- または、`HrApiClient` 側で `requests` の例外を捕捉してボディを抽出する
- どちらの方式が良いかは実装時に判断するが、設計としては「エラー時にもレスポンスボディを読める」ことを要件にする

#### 入力エラー

| パターン | 対応 |
|---|---|
| `yyyy-mm` フォーマット不正 | エラーメッセージを出して終了コード `2` |
| 存在しない年月 (例: `2026-13`) | エラーメッセージを出して終了コード `2` |
| 入力が空 | エラーメッセージを出して終了コード `2` |

#### ID 取得失敗

| パターン | 対応 |
|---|---|
| `companies` 配列が空 | `ActionExecutionError` を投げる。終了コード `1` |
| 「出社」タグが見つからない | `ActionExecutionError` を投げる。終了コード `1` |

### 終了コード

001 の方針に従う。

| コード | 条件 |
|---|---|
| `0` | 全日付の処理が正常完了 |
| `1` | API エラー、ID 取得失敗、権限不足、接続エラー |
| `2` | `yyyy-mm` フォーマット不正、入力エラー |
| `130` | Ctrl+C によるユーザー中断 |

### コンソールログ出力の設計

1 件ごとに進捗を表示する。要望に「1件終わるごとにコンソールログがほしい。エラーもわかりやすく。」とある。

#### 正常系ログ

```text
対象月: 2026-03 (31日間)

[SKIP] 2026-03-01 (prescribed_holiday)
[OK]   2026-03-02 09:00-19:00 出社タグ付与済み
[OK]   2026-03-03 09:00-19:00 出社タグ付与済み
[OK]   2026-03-04 09:00-19:00 出社タグ付与済み
[OK]   2026-03-05 09:00-19:00 出社タグ付与済み
[OK]   2026-03-06 09:00-19:00 出社タグ付与済み
[SKIP] 2026-03-07 (prescribed_holiday)
[SKIP] 2026-03-08 (prescribed_holiday)
...

完了: 22日登録 / 9日スキップ / 0日エラー
```

#### エラー発生時のログ

```text
対象月: 2026-03 (31日間)

[SKIP] 2026-03-01 (prescribed_holiday)
[OK]   2026-03-02 09:00-19:00 出社タグ付与済み
[ERR]  2026-03-03 給与明細が確定されているので、更新できません。

中断: 1日登録 / 1日スキップ / 1日エラー (2026-03-03 で中断)
```

#### タグ付与でエラーした場合のログ

```text
[OK]   2026-03-02 09:00-19:00 勤怠登録済み
[ERR]  2026-03-02 タグ付与失敗: <エラーメッセージ>

中断: 0日登録 / 0日スキップ / 1日エラー (2026-03-02 で中断)
```

注意: 勤怠登録は成功したけどタグ付与で失敗した場合、勤怠だけ入った状態になる。これは仕様として許容する。再実行時に勤怠の上書きは問題ないので、再実行でリカバリー可能。

#### ログ出力の実装方針

- `print()` で標準出力に出す (ロギングライブラリは使わない。シンプルに)
- 色付けは今回はやらない (将来的に追加してもよい)

### 005 との差分まとめ

| 項目 | 005 | 006 |
|---|---|---|
| 対象日数 | 1 日 | 月の全日 |
| API 構造 | 未確認 (三段階方式) | curl で確認済み (固定 payload) |
| day_pattern 判定 | なし | あり (`normal_day` のみ処理) |
| 事前取得モード | あり | なし (API 構造確定済みなので不要) |
| .env 反映 | あり | なし (`users/me` で毎回取得する) |
| 勤怠タグ | 勤怠タグ + 日時タグ (別管理の可能性を考慮) | 勤怠タグのみ (curl 確認済みの API を使う) |
| エラー時の挙動 | 設計のみ | ループ即中断 |
| コンソールログ | 要件なし | 1 件ごとに進捗表示 |

### 将来の拡張余地

- 時刻の可変化: `09:00` と `19:00` を引数や設定から取れるようにする
- 休憩時刻の可変化: `11:30-12:30` も同様
- リトライ: API の一時的な 5xx エラーに対するリトライ
- ドライランモード: 実際に PUT せず、処理対象の日付一覧だけ出力する
- 複数会社対応: `companies` が複数ある場合の選択 UI

## レビュー
